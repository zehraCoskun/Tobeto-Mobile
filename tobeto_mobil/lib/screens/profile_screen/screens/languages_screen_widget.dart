import 'package:flutter/material.dart';

class LanguagesWidget extends StatelessWidget {
  const LanguagesWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Yabancı Dillerim'),
    );
  }
}