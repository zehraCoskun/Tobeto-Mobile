import 'package:flutter/material.dart';

class CertificatesWidget extends StatelessWidget {
  const CertificatesWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Sertifikalarım'),
    );
  }
}