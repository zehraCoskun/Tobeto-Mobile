import 'package:flutter/material.dart';

class CompetencesWidget extends StatelessWidget {
  const CompetencesWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Yetkşnliklerim'),
    );
  }
}