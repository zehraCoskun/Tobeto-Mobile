import 'package:flutter/material.dart';

class EducationsWidget extends StatelessWidget {
  const EducationsWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Eğitim Hayatım'),
    );
  }
}
