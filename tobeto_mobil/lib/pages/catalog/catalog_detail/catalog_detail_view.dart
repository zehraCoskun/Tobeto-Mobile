import 'package:flutter/material.dart';

class CatalogDetailView extends StatelessWidget {
  const CatalogDetailView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}