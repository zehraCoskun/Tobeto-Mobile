import 'package:flutter/material.dart';
import 'package:tobeto_mobil/pages/home/survey_view/no_survey_card.dart';

class SurveyView extends StatelessWidget {
  const SurveyView({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const NoSurveyCard();
  }
}